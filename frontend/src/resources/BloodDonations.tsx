//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const BloodDonationsTitle = () => {
  const record = useRecordContext();
  return <span>BloodDonations {record ? `"${record.donationDate}"` : ''}</span>;
};

export const BloodDonationsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <ReferenceField source="userId" reference="Users" />
      <DateField source="donationDate" />
      <TextField source="location" />
      <TextField source="typeOfDonation" />
      <TextField source="notes" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const BloodDonationsEdit = () => (
  <Edit title={<BloodDonationsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="userId" reference="Users" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="donationDate" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="location" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="typeOfDonation" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="notes" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const BloodDonationsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <ReferenceInput source="userId" reference="Users" />
        </Grid>
        <Grid item xs={4}>
          <DateInput source="donationDate" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="location" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="typeOfDonation" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="notes" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  <ReferenceInput source="userId" label="userId" reference="Users" alwaysOn />,
  ,
  ,
  ,
  ,
];
